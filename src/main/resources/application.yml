server:
  port: 8080
  error:
    include-message: always

spring:
  main:
    web-application-type: servlet
  datasource:
    url: jdbc:postgresql://localhost:5432/ztpprojekt2
    username: ztpprojekt
    password: ztpprojekt
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: false
    generate-ddl: true
    defer-datasource-initialization: true
  sql:
    init:
      mode: always

